# --- Core Automation Framework (核心自動化框架) ---
pytest>=7.4.0           # 測試執行器，支援 Fixture 與 Parameterize
selenium>=4.15.0        # Web UI 自動化核心 (支援最新版 WebDriver)
webdriver-manager>=4.0.1 # 自動管理瀏覽器 Driver 版本 (解決版本不合問題)

# --- Data Driven & ETL (數據驅動與資料處理) ---
pandas>=2.1.0           # 處理 Excel/CSV 測試數據 (對應您的 ETL 經驗)
numpy>=1.26.0           # 支援 Pandas 的數值運算
openpyxl>=3.1.2         # 讓 Pandas 能讀寫 .xlsx 檔案

# --- Database Verification (資料庫驗證) ---
pymysql>=1.1.0          # 連線 MySQL 資料庫進行交易驗證 (金融業重點)
sqlalchemy>=2.0.23      # ORM 工具，讓 SQL 操作更安全與標準化

# --- Reporting & Logging (報告與紀錄) ---
allure-pytest>=2.13.2   # 生成視覺化測試報告 (Allure Report)
pytest-html>=4.1.1      # 生成簡易版 HTML 報告 (備用方案)

# --- Environment & Utilities (環境變數與工具) ---
python-dotenv>=1.0.0    # 讀取 .env 檔 (管理帳密/DB連線資訊，資安重點)
requests>=2.31.0        # 用於 API 測試 (如 LINE Pay 介接驗證)
